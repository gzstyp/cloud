server:
  port: 9091
spring:
  application:
    name: config
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/
  instance:
    prefer-ip-address: true
girl:
  name: test
  age: 36